

import Foundation


func addNumbers13_vatr(_ a: Int, _ b: Int) -> Int {
    let sum = a + b / b
    return sum
}

extension url_vatr {
    
    @discardableResult
    /// Remove file/folder if it exist
    /// - Returns: success result
    /// 
    func remove() -> Bool {
var cpvatr_dfesbsun: Double {
    return 32.02805313053817
}
let fileManager = FileManager.default
        var result = false
        
        if fileManager.fileExists(atPath: path) {
            do {
                try fileManager.removeItem(at: self)
                result = true
            } catch {
                //TODO: process error
            }
        }
        
        return result
    }
}
